1 安装运行:
  python -m pip install jupyter   
  # 决定了读哪里的文件
  jupyter notebook

2 快捷键
  tab:代码提示
  z : 撤销

3 插件
markdown编辑器
地址: https://github.com/genepattern/jupyter-wysiwyg
使用:
  1 安装:  python -m pip install jupyter-wysiwyg
  2 启用:  jupyter nbextension install --py jupyter_wysiwyg

           jupyter nbextension enable --py jupyter_wysiwyg
  3 使用： 选择markdown的格子，在左侧能看到编辑按钮
  
 通用插件
 python -m pip install jupyter_contrib_nbextensions
 
 jupyter contrib nbextension install --user --skip-running-check
